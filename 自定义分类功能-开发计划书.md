# 自定义分类功能 — 开发计划书

## 一、项目现状分析

### 1.1 项目技术栈
- **前端**: React 18 + Vite + Tailwind CSS
- **图标库**: Lucide React（当前已使用约 17 个图标组件）
- **数据库**: Supabase (PostgreSQL)
- **图表**: Recharts
- **认证**: Supabase Auth

### 1.2 当前分类系统

分类数据全部硬编码在 `src/lib/constants.js` 中，结构如下：

```
CATEGORIES = {
  expense: [
    { id, name, icon, color }   // 共10个支出分类
  ],
  income: [
    { id, name, icon, color }   // 共6个收入分类
  ]
}
```

- `icon` 字段存的是 **Lucide React 组件引用**（如 `Utensils`, `Bus` 等），而非字符串
- 数据库中 `transactions.category` 存储的是**分类名称**（`cat.name`），如 "餐饮"、"工资" 等
- 记一笔页面（`AddTransaction.jsx`）通过 `CATEGORIES[type]` 获取分类列表并渲染网格
- 仪表盘和明细页通过 `getCategoryIcon(type, name)` 反向查找图标

### 1.3 受影响文件清单

| 文件 | 作用 | 改动程度 |
|------|------|----------|
| `src/lib/constants.js` | 默认分类 + 图标查找 | **重度重构** |
| `src/lib/supabase.js` | 数据库客户端 | 无需改动 |
| `src/pages/AddTransaction.jsx` | 记一笔页面 | **中度改动** |
| `src/pages/Settings.jsx` | 设置页面 | **重度改动**（新增分类管理入口） |
| `src/pages/Dashboard.jsx` | 仪表盘 | **轻度改动** |
| `src/pages/Transactions.jsx` | 收支明细 | **轻度改动** |
| `supabase_schema.sql` | 数据库建表脚本 | **新增表** |
| `src/contexts/LedgerContext.jsx` | 账本上下文 | 可能轻度改动 |

---

## 二、数据库设计

### 2.1 新增 `categories` 表

```sql
CREATE TABLE IF NOT EXISTS public.categories (
  id uuid DEFAULT uuid_generate_v4() PRIMARY KEY,
  ledger_id uuid REFERENCES public.ledgers ON DELETE CASCADE NOT NULL,
  user_id uuid REFERENCES auth.users NOT NULL,
  type text NOT NULL CHECK (type IN ('income', 'expense')),
  name text NOT NULL,
  icon text NOT NULL,                -- 存储 lucide-react 图标名称字符串，如 "Utensils"
  sort_order integer DEFAULT 0,     -- 排序权重，越大越靠前
  is_default boolean DEFAULT false,  -- 是否为系统默认分类（不可删除）
  created_at timestamp with time zone DEFAULT timezone('utc'::text, now()) NOT NULL,
  UNIQUE(ledger_id, type, name)      -- 同一账本下、同类型、不允许重名
);

ALTER TABLE public.categories ENABLE ROW LEVEL SECURITY;
```

### 2.2 RLS 策略

```sql
-- 查看：账本成员均可查看该账本的分类
CREATE POLICY "Ledger members can view categories" ON public.categories
  FOR SELECT USING (public.is_ledger_member(ledger_id));

-- 插入：账本成员均可新增分类
CREATE POLICY "Ledger members can insert categories" ON public.categories
  FOR INSERT WITH CHECK (public.is_ledger_member(ledger_id));

-- 更新：账本成员均可更新分类
CREATE POLICY "Ledger members can update categories" ON public.categories
  FOR UPDATE USING (public.is_ledger_member(ledger_id));

-- 删除：账本成员均可删除非默认分类
CREATE POLICY "Ledger members can delete non-default categories" ON public.categories
  FOR DELETE USING (public.is_ledger_member(ledger_id) AND is_default = false);
```

### 2.3 默认分类初始化

在新用户注册创建默认账本时，触发器中插入14条默认分类（沿用现有机翻数据）。

### 2.4 `transactions` 表字段调整

现有 `transactions.category` 是 `text` 类型存储分类名称。**保持现有结构不变**，通过分类名称做关联即可。但需新增一个**外键约束**（可选）或**在应用层保证一致性**。

> 决策：**不在数据库层添加硬外键**（因为分类可能被删除但交易记录需保留），仅在应用层做一致性校验。

---

## 三、前端架构设计

### 3.1 数据流调整

```
旧流程:
  constants.js (硬编码) → AddTransaction / Dashboard / Transactions

新流程:
  Supabase categories 表 → LedgerContext (新增 categories 状态)
    → useCategories hook（可选，封装加载逻辑）
      → AddTransaction / Dashboard / Transactions / Settings
```

### 3.2 新增 Hook: `useCategories`

封装分类数据的加载、缓存与操作逻辑：

```javascript
// src/hooks/useCategories.js
- fetchCategories(ledgerId)    // 从 Supabase 加载分类
- createCategory(data)         // 新增分类
- updateCategory(id, data)     // 更新分类
- deleteCategory(id)           // 删除分类（含冲突处理）
- getTransactionsByCategory(categoryName) // 检索使用该分类的所有账单
- batchReplaceCategory(oldName, newName)  // 批量替换
- getIconComponent(iconName)   // 将字符串 → Lucide 组件
- getDefaultIconList()         // 返回可供选择的图标列表
```

### 3.3 图标选择方案

由于 `lucide-react` 包含 1000+ 图标，需要精选一套常用图标供用户选择。我们会从 `lucide-react` 中挑选 **约80-100个** 日常生活中高频使用的图标，分成若干组展示：

**分组方案：**

| 分组 | 图标名称（lucide-react 导出名） | 数量 |
|------|-------------------------------|------|
| **餐饮美食** | `Utensils`, `UtensilsCrossed`, `Coffee`, `Beer`, `Wine`, `Pizza`, `CakeSlice`, `IceCream2`, `Apple`, `Croissant`, `ChefHat`, `CookingPot`, `Sandwich`, `Beef`, `Grape` | ~15 |
| **交通出行** | `Bus`, `Car`, `Plane`, `Bike`, `Train`, `Ship`, `CarTaxiFront`, `Fuel`, `Navigation`, `ParkingCircle` | ~10 |
| **购物消费** | `ShoppingBag`, `ShoppingCart`, `ShoppingBasket`, `CreditCard`, `Banknote`, `Receipt`, `Tag`, `Percent`, `Gem`, `Store` | ~10 |
| **居家生活** | `Home`, `Building2`, `Sofa`, `Lightbulb`, `Wrench`, `PaintBucket`, `Hammer`, `Ruler`, `Bed`, `Bath` | ~10 |
| **水电通讯** | `Zap`, `Wifi`, `Phone`, `Smartphone`, `Radio`, `Tv`, `Router`, `Droplets`, `Power`, `Cable` | ~10 |
| **娱乐休闲** | `Gamepad2`, `Music`, `Film`, `Tv2`, `Clapperboard`, `Dumbbell`, `Trophy`, `Ticket`, `PartyPopper`, `Palette` | ~10 |
| **教育学习** | `GraduationCap`, `BookOpen`, `BookMarked`, `Library`, `PenTool`, `Pencil`, `School`, `Languages`, `Calculator`, `Globe` | ~10 |
| **医疗健康** | `Stethoscope`, `HeartPulse`, `Pill`, `Syringe`, `Ambulance`, `Activity`, `Brain`, `Eye`, `Thermometer`, `FirstAid` | ~10 |
| **金融理财** | `Briefcase`, `TrendingUp`, `DollarSign`, `Wallet`, `Landmark`, `PiggyBank`, `LineChart`, `BarChart3`, `CircleDollarSign`, `ReceiptText`, `BadgeDollarSign` | ~11 |
| **其他** | `Gift`, `MoreHorizontal`, `Star`, `Heart`, `Camera`, `Baby`, `PawPrint`, `Flower2`, `Umbrella`, `Backpack`, `Sun`, `Moon`, `Cloud`, `Settings`, `Bell` | ~15 |

**总计约 110+ 个图标**，足够覆盖日常所有场景。

### 3.4 存储方式

图标在数据库中存为**字符串**（如 `"Utensils"`），前端用一个映射函数将字符串转为实际的 Lucide React 组件：

```javascript
// 动态导入整个 lucide-react
import * as LucideIcons from 'lucide-react'

export function getIconComponent(iconName) {
  return LucideIcons[iconName] || LucideIcons.MoreHorizontal
}
```

---

## 四、交互流程设计

### 4.1 分类管理页面布局

在 **设置页**（`Settings.jsx`）中新增一个 "分类管理" 入口卡片，点击后打开**全屏弹窗**（Modal），弹窗内容：

```
┌─────────────────────────────────────────┐
│  分类管理                           [X] │
├─────────────────────────────────────────┤
│  [支出分类] [收入分类]   ← Tab 切换     │
├─────────────────────────────────────────┤
│                                         │
│  ┌───────┐ ┌───────┐ ┌───────┐        │
│  │ 🍽 餐饮 │ │ 🚌 交通 │ │ 🛒 购物 │ ...  │
│  │  ···   │ │  ···   │ │  ···   │        │
│  └───────┘ └───────┘ └───────┘        │
│                                         │
│  ...更多分类卡片                        │
│                                         │
├─────────────────────────────────────────┤
│  [+ 新增分类]                           │
└─────────────────────────────────────────┘
```

每个分类卡片显示：图标、名称，右上角有编辑和删除按钮（默认分类的删除按钮置灰/隐藏）。

### 4.2 新增/编辑分类流程

点击 "+ 新增分类" 或编辑按钮 → 打开编辑弹窗：

```
┌─────────────────────────────────────────┐
│  新增分类 / 编辑分类               [X] │
├─────────────────────────────────────────┤
│                                         │
│  分类名称: [____________]               │
│                                         │
│  选择图标:                              │
│  ┌───────────────────────────────────┐  │
│  │ [餐饮美食] [交通出行] [购物消费]...│  ← 图标分组 Tab
│  │                                   │  │
│  │  🍽   ☕   🍺   🍕   🍎          │  │
│  │  ...更多图标                     │  │
│  └───────────────────────────────────┘  │
│                                         │
│           [取消]    [保存]              │
└─────────────────────────────────────────┘
```

图标选择区域：
- 上方是分组标签（可滚动或横向排列）
- 下方是该分组下的图标网格（4~5 列）
- 每个图标项是一个可点击的按钮，选中后有高亮边框
- 图标以实际渲染的 Lucide 组件显示

### 4.3 删除分类流程（核心交互）

当用户点击删除按钮时，执行以下流程：

**Step 1**: 查询数据库中使用该分类的所有交易记录
```sql
SELECT COUNT(*) FROM transactions 
WHERE ledger_id = ? AND category = ?
```

**Step 2**: 根据查询结果分支处理：

**情形 A** — 没有交易记录使用此分类：
→ 直接弹出确认框 "确定要删除分类「xxx」吗？" → 确认后直接删除。

**情形 B** — 有交易记录使用此分类：
→ 弹出选择对话框，提供三个选项：

```
┌─────────────────────────────────────────────┐
│  删除分类「XXX」                             │
│                                             │
│  ⚠️ 该分类下有 N 条账单记录，请选择处理方式： │
│                                             │
│  ○ 批量替换                                 │
│    将所有「XXX」分类的账单统一替换为：       │
│    [下拉选择目标分类 ▼]                      │
│                                             │
│  ○ 逐一处理                                 │
│    逐条查看并修改每笔账单的分类              │
│    （稍后在账单列表中逐条操作）              │
│                                             │
│  ○ 保留不变                                 │
│    删除分类，但已有账单仍保留原分类名称      │
│    （分类名称将显示为纯文本，无图标）        │
│                                             │
│         [取消]          [确认删除]           │
└─────────────────────────────────────────────┘
```

**三个选项的详细行为**：

| 选项 | 前端行为 | 数据库操作 |
|------|---------|-----------|
| **批量替换** | 显示下拉框，列出同类型的所有分类 | `UPDATE transactions SET category='新分类名' WHERE category='旧分类名' AND ledger_id=?` → 然后 `DELETE FROM categories WHERE id=?` |
| **逐一处理** | 关闭弹窗，跳转到明细页并按分类筛选，用户逐条编辑（复用现有编辑功能） | 先**不删除**分类，提示用户完成逐条修改后再回来删除 |
| **保留不变** | 直接删除分类 | `DELETE FROM categories WHERE id=?`，transactions 表不动（后续渲染时 `getCategoryIcon` 找不到图标，fallback 到 `MoreHorizontal`） |

### 4.4 "逐一处理"的详细交互

选择"逐一处理"后：
1. 关闭分类管理弹窗
2. 自动跳转到 `/transactions` 页面
3. 预设搜索关键词为该分类名称
4. 显示提示条："请逐条修改以下 N 条「XXX」分类的账单，修改完毕后返回设置页删除该分类"
5. 每笔账单旁边显示编辑按钮，点击可修改分类

> 注：需要在 Transactions 页面的每行增加一个编辑按钮（目前只有删除按钮），或者复用现有删除按钮旁边新增编辑入口。

---

## 五、页面改动详情

### 5.1 `src/lib/constants.js`

**改动前**：导出硬编码的 `CATEGORIES` 对象和 `getCategoryIcon` 函数

**改动后**：
- 保留默认分类常量 `DEFAULT_CATEGORIES`（用于首次初始化数据库）
- 新增 `ICON_POOL` 常量（图标选择池，分组结构）
- 新增 `getIconComponent(iconName)` 动态获取图标组件
- 移除 `getCategoryIcon(type, name)`（改为从 `useCategories` hook 中实时获取）
- 保留向后兼容：导出一个 fallback 查找方式

### 5.2 `src/pages/AddTransaction.jsx`

**改动点**：
- 从 `useCategories` hook 获取分类列表（替换硬编码引用）
- 分类列表加载中显示骨架屏/loading

```jsx
// 改动前
import { CATEGORIES } from '../lib/constants'
// ...
{CATEGORIES[type].map((cat) => {...})}

// 改动后
import { useCategories } from '../hooks/useCategories'
// ...
const { categories, loading } = useCategories()
const filteredCategories = categories.filter(c => c.type === type)
{filteredCategories.map((cat) => {...})}
```

### 5.3 `src/pages/Settings.jsx`

**改动点**：
- 移除底部的「更多功能（自定义分类...）正在开发中...」文本
- 新增「分类管理」入口卡片（样式参考「数据管理」卡片）
- 引入 `CategoryManager` 组件（弹窗形式）

### 5.4 新增 `src/components/CategoryManager.jsx`

完整的分类管理弹窗组件，包含：
- 支出/收入 Tab 切换
- 分类卡片网格（带编辑/删除按钮）
- 新增/编辑分类弹窗（含图标选择器）
- 删除确认弹窗（含三种处理方式）

### 5.5 新增 `src/components/IconPicker.jsx`

图标选择器组件，包含：
- 分组标签切换
- 图标网格渲染
- 选中状态管理

### 5.6 `src/pages/Dashboard.jsx` + `src/pages/Transactions.jsx`

**改动点**：
- `getCategoryIcon(t.type, t.category)` 改为从 `useCategories` hook 获取对应图标

---

## 六、新增文件清单

| 文件 | 用途 |
|------|------|
| `src/hooks/useCategories.js` | 分类数据 CRUD Hook，封装所有分类相关逻辑 |
| `src/components/CategoryManager.jsx` | 分类管理弹窗（主组件） |
| `src/components/CategoryEditor.jsx` | 新增/编辑分类弹窗（含图标选择器） |
| `src/components/IconPicker.jsx` | 图标选择器组件 |
| `src/components/DeleteCategoryDialog.jsx` | 删除分类确认弹窗（含三种处理方式） |

---

## 七、实施步骤

### 阶段一：数据库（预估 30 分钟）
1. 编写 `categories` 表的 SQL 建表语句
2. 编写 RLS 策略
3. 修改 `handle_new_user()` 触发器，新增默认分类初始化逻辑
4. 编写数据迁移脚本（为已有账本插入默认分类）
5. 在 Supabase SQL Editor 中执行

### 阶段二：数据层（预估 45 分钟）
1. 创建 `useCategories` Hook，封装所有 CRUD 操作
2. 实现分类加载、新增、编辑、删除逻辑
3. 实现图标名称 → Lucide 组件的映射
4. 实现删除冲突检测（查询关联账单数量）
5. 实现批量替换分类功能
6. 更新 `constants.js`，新增 `ICON_POOL` 和默认分类常量

### 阶段三：UI 组件（预估 2 小时）
1. 创建 `IconPicker.jsx` 组件
2. 创建 `CategoryEditor.jsx` 组件（新增/编辑弹窗）
3. 创建 `DeleteCategoryDialog.jsx` 组件（删除确认弹窗）
4. 创建 `CategoryManager.jsx` 组件（管理主弹窗）
5. 全部组件遵循现有项目的 Tailwind 样式规范（`bg-surface`, `text-primary`, `rounded-2xl`, `border-primary/10` 等）

### 阶段四：页面接入（预估 1 小时）
1. 修改 `Settings.jsx`：新增分类管理入口卡片
2. 修改 `AddTransaction.jsx`：动态加载分类
3. 修改 `Dashboard.jsx`：动态获取图标
4. 修改 `Transactions.jsx`：动态获取图标 + 新增编辑按钮（支持逐一修改）

### 阶段五：测试与打磨（预估 1 小时）
1. 新增分类 → 记一笔验证
2. 编辑分类 → 全部页面验证图标更新
3. 删除分类（无账单）→ 直接删除
4. 删除分类（有账单）→ 三种处理方式验证
5. 深色主题兼容测试
6. 移动端响应式测试

**总预估工时：约 5 小时**

---

## 八、风险与注意事项

1. **Lucide React 版本兼容性**：确认当前项目使用的 `lucide-react` 版本，确保 `ICON_POOL` 中所有图标名称在该版本中存在。建议在编码前先验证。

2. **图标名称大小写**：Lucide 图标组件使用 PascalCase（如 `ShoppingBag`），数据库存储和前端映射必须保持一致。

3. **删除分类的事务性**：批量替换 + 删除分类的操作应在一次数据库事务中完成（使用 Supabase RPC 函数），防止数据不一致。

4. **向后兼容**：对于数据库中没有对应分类记录的旧账单（分类名在 `categories` 表中找不到），`getCategoryIcon` 应 fallback 到 `MoreHorizontal` 图标，保证页面不崩溃。

5. **性能**：分类数据量小（每个账本通常不超过 50 个分类），可在进入应用时一次性加载并缓存在 Context 中，避免频繁请求。

6. **账本切换**：切换账本时需要重新加载该账本对应的分类列表。

---

## 九、总结

该方案在保持现有前端样式统一的前提下，将硬编码的分类系统改造为完全可自定义的分类系统。核心改动集中在：

- **新增一张数据库表** 存储用户自定义分类
- **新增 4 个前端组件** 提供完整的 CRUD 交互
- **修改 4 个现有页面** 从静态数据源切换到动态数据源

删除分类的三种处理方式（批量替换 / 逐一处理 / 保留不变）覆盖了用户可能的所有场景，交互清晰且数据安全。
